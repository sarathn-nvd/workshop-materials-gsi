# Production-mimic data pipeline + runtime
# Pinned where stability matters; loose elsewhere.

# Core
pydantic>=2.7,<3
pyyaml>=6.0,<7

# Data
pandas>=2.2,<3
pyarrow>=15,<19
numpy>=1.26,<3

# Postgres (Tool 1 + Tool 2 backing stores)
psycopg[binary]>=3.1,<4
sqlalchemy>=2.0,<3

# Vector search (Tool 4 RAG)
pgvector>=0.3,<0.5

# Sanctions name matching (Tool 3)
rapidfuzz>=3.6,<4

# Services (Tool 3 / 4 / 5)
fastapi>=0.110,<1
uvicorn[standard]>=0.30,<1

# LLM client (OpenAI-compatible to vLLM / NIM)
openai>=1.30,<2
httpx>=0.27,<1
tenacity>=8,<10

# Async parallelism
anyio>=4,<5

# Tooling / dev
pytest>=8,<9
ruff>=0.4
